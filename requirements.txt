# Система учёта иностранных студентов ННГАСУ
# Зависимости

# Обязательно для генерации Word-документов (Справка, Визовая анкета)
python-docx>=1.1.0

# Встроенные в Python (не нужно устанавливать):
# tkinter   — графический интерфейс
# sqlite3   — база данных
# hashlib   — хэширование паролей
# secrets   — генерация соли
# datetime  — работа с датами
# calendar  — валидация дат
